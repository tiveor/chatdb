# PostgreSQL connection string (Supabase, Neon, local, etc.)
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# OpenAI-compatible API endpoint (LM Studio, Ollama, vLLM, etc.)
OLLAMA_URL=http://localhost:1234

# Optional: force a specific model (otherwise uses the first available)
# OLLAMA_MODEL=qwen2.5-coder-7b

# Optional: server port (default: 3001)
# PORT=3001
